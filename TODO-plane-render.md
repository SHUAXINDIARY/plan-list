# 飞机模型视窗 TODO 技术方案

> 状态：T1-T7 已完成。本文补充 [现有视窗技术方案](./docs/aircraft-model-viewport.md)，不替代其中已经确认的 WebGPU、单模型加载和资源释放设计。

## 目标

将 `/plane-render` 从“可以操作的模型预览”提升为可靠的模型检查工具：任何机型、姿态、屏幕尺寸和加载失败场景下，用户都能理解当前状态、恢复到可用视图，并以可预测的性能完成观察与调节。

## 范围与约束

- 保持每次仅加载一架 GLB、模型目录选择、页面级加载状态和当前加载遮罩。
- 保持 WebGPU 为默认渲染路径；WebGL 降级属于独立产品决策，不进入本轮必做范围。
- 不修改 GLB、纹理或现有模型目录结构。
- 不新增大型依赖；优先使用现有 Three.js、React 和 CSS 能力。
- 全屏必须覆盖实际可视窗口，保留可见退出入口、Esc 退出和焦点返回。
- 所有交互继续满足键盘可用、可见焦点、减少动态偏好和窄屏可操作。

## 当前基线

当前实现已具备以下能力，后续改动不得回归：

- `AircraftModelViewport` 内部初始化 `WebGPURenderer`，并在模型切换或卸载时释放场景资源。
- 页面外层负责模型目录和加载状态，视窗内部加载遮罩覆盖真实画布。
- 支持轨道相机、姿态预设、姿态拖拽、渲染参数、展示平面和全屏查看。
- `ResizeObserver` 根据容器尺寸更新相机投影和渲染缓冲区。

## 实施顺序

| 优先级 | 任务 | 状态 | 依赖 | 交付结果 |
| --- | --- | --- | --- | --- |
| P0 | T1 空间变换与相机取景 | 已完成 | 无 | 任意姿态下模型不漂移或被裁切，可检查机腹 |
| P0 | T2 全屏与窄屏工具层 | 已完成 | 无 | 全屏信息完整，移动端面板不裁切、不重叠 |
| P1 | T3 状态、失败恢复与加载反馈 | 已完成 | T2 | 用户可理解后端、加载阶段与失败后的下一步 |
| P1 | T4 操作语义与无障碍 | 已完成 | T1、T2 | 相机和姿态职责清楚，键盘操作完整 |
| P1 | T5 性能与画质预算 | 已完成 | T1、T3 | 静止状态不持续消耗 GPU，低性能设备可用 |
| P2 | T6 画面质感与照明预设 | 已完成 | T5 | 材质、阴影和背景更适合模型检查 |
| P2 | T7 渲染后产出能力 | 已完成 | T3、T4 | 可导出、复用或分享当前检查结果 |

## T1 空间变换与相机取景

**状态：已完成。**

### 问题

- 当前模型归一化后以包围盒底部贴地，姿态直接写入同一个模型根节点。俯仰和滚转会围绕地面基准旋转，容易产生视觉漂移、穿过展示平面或相机目标偏移。
- 相机最大极角限制在接近水平线，无法完整检查机腹、发动机下表面和起落架。
- 初始相机距离由固定倍数计算，不能可靠适配细长机身、宽翼展、竖屏和全屏比例。

### 方案

1. 将“模型归一化”和“姿态旋转”拆到两个对象层级：

   ```text
   aircraftStage
   ├── aircraftAttitudePivot  # 以机体几何中心或明确重心为旋转原点
   │   └── normalizedModel    # 仅负责缩放和居中
   └── displayFloor           # 仅作为展示参考，不承诺非平飞时仍接触起落架
   ```

2. 归一化时将 `normalizedModel` 的包围盒中心移至 pivot 原点；展示平面按平飞时的底部高度定位。非平飞状态下，地面只表达空间参考，不以伪物理方式强行追随模型。
3. 明确航空姿态的轴向与组合顺序，以单一 quaternion 或具名 Euler 顺序作为唯一来源。完成后用“平飞、起飞、下降、左滚转、右偏航”五个固定姿态核对方向和 gizmo 表达一致性。
4. 用包围球和相机垂直/水平 FOV 计算 fit 距离，保留固定安全边距；模型、窗口尺寸和全屏状态变化后均可重新 fit。
5. 把相机范围放宽至上下均可观察，但在两端保留小角度限制以避免控制翻转；新增正面、侧面、顶部、底部和“适配视图”命令。
6. 分离三个恢复命令：恢复相机、恢复姿态、恢复工作室。后者才同时恢复视角、姿态和渲染参数，避免一个重置按钮意外抹掉用户的其他设置。

### 影响文件

- `src/pages/planeRender/AircraftModelViewport.tsx`
- `src/pages/planeRender/index.css`

### 验收条件

- 在 `pitch = -60..60`、`roll = -180..180`、`yaw = -180..180` 下，模型中心与相机目标关系稳定，不发生非预期裁切。
- 顶视、底视、前视、侧视和适配视图均可在鼠标、触摸和键盘下恢复。
- 切换横屏、竖屏、全屏和 320px 宽度后，适配视图仍包含完整机身。

### 本次实现

- `normalizeAircraftModel` 现在将三轴几何中心移到原点，模型挂载到独立的 `aircraftAttitudePivot` 后再应用姿态。
- 姿态使用 `YXZ` Euler 顺序；相机允许上下观察，并以包围球和当前视口 FOV 计算 fit 距离。
- 工具条新增自定义、适配、正面、侧面、顶部和底部视角菜单；轨道手动操作后标记为自定义。

## T2 全屏与窄屏工具层

**状态：已完成。**

### 问题

- 当前全屏目标是视窗内部 canvas 容器，外层的模型名称和页面加载状态不会进入全屏树。
- 渲染控制与姿态控制可以同时展开，且每个面板按各自按钮绝对定位。窄屏时左侧面板可能被 viewport 的 `overflow: hidden` 裁掉。
- `touch-action: none` 放在整个 viewport，可能影响工具面板和页面的自然触摸滚动。

### 方案

1. 将全屏目标提升到包含 canvas、工具层、模型名称和状态反馈的完整 viewport。由页面层或明确的 fullscreen target ref 统一管理，避免跨组件寻找 DOM。
2. 进入全屏后将可见的“退出全屏”按钮设为首个可达控制，记录进入前的触发元素，退出后恢复焦点；浏览器 `fullscreenchange` 仍为状态唯一来源。
3. 将工具面板收敛为单一状态：`none | render | attitude`。打开任一面板时关闭另一面板，Esc 和点击画布空白处关闭面板。
4. 桌面端面板可锚定工具条，移动端改为相对于完整 viewport 的底部面板或全宽抽屉。面板最大高度使用 `100dvh` 与安全区计算，内部独立滚动。
5. 仅让 Three canvas 和姿态直接操控区域使用 `touch-action: none`；工具面板、滚动内容和页面其余区域保留浏览器默认手势。
6. 在全屏、加载和错误状态下，工具层、关闭按钮、错误信息和模型元信息的 z-index 顺序必须固定且互不遮挡。

### 影响文件

- `src/pages/planeRender/index.tsx`
- `src/pages/planeRender/AircraftModelViewport.tsx`
- `src/pages/planeRender/index.css`

### 验收条件

- 全屏时画布覆盖 `100vw` 和 `100dvh`，没有页面标题、目录或状态文字留在可视区外。
- 320px 宽和横向手机高度下，任意时刻最多一个面板展开，面板内容均可滚动到末尾。
- 触碰 canvas 可旋转模型，触碰面板仍可滚动、拖动 range 和操作原生 select。
- 通过按钮、Esc 和浏览器原生退出全屏后，焦点均回到进入按钮。

### 本次实现

- 页面级 `.plane-render__viewport` 作为全屏目标，画布、工具层、状态和模型 caption 一起进入全屏；全屏尺寸使用 `100vw` / `100dvh`。
- 两个工具面板互斥，Esc 和点击画布空白区域会收起面板；窄屏工具条允许换行，面板相对于工具层定位。
- `touch-action: none` 仅保留在 Three canvas 和姿态直接操控区，页面 viewport 与工具面板不再被整体锁定。

## T3 状态、失败恢复与加载反馈

**状态：已完成。**

### 方案

1. 将页面文案从“正在载入模型目录 / 模型目录已载入”改为“正在载入当前模型 / 当前模型已就绪”，目录数量继续由侧栏展示。
2. 扩展加载进度语义，至少区分：后端检测、渲染器初始化、资源下载、GLB 解析、场景就绪、模型失败和设备不可用。下载不可获得有效总字节时使用不定量状态，不显示虚假百分比。
3. 错误状态显示当前模型标签和可读原因，提供“重试当前模型”命令；重试必须重新初始化失败的 renderer 或重新请求资源，不能复用可能失效的对象。
4. 页面状态栏显示真实后端结果，例如 `WebGPU 已就绪`、`WebGPU 不可用`，不再在失败时固定显示 `WebGPU`。
5. 为 WebGPU device/context 丢失准备可恢复状态：停止渲染、显示错误、释放旧对象并允许用户触发重新初始化。
6. WebGL 回退不在本轮实现。若未来产品决定支持，应单独定义画质差异、兼容范围、错误文案和验收矩阵，不能静默降级。

### 影响文件

- `src/pages/planeRender/AircraftModelViewport.tsx`
- `src/pages/planeRender/index.tsx`
- `src/pages/planeRender/index.css`

### 验收条件

- 正常加载、空目录、损坏 GLB、模型 URL 失败、WebGPU 不可用和初始化失败都有不同且可读的状态。
- 所有失败状态均给出恢复动作或明确说明不可恢复原因。
- 重试后不会留下旧 canvas、重复动画循环或已释放资源的引用。

### 本次实现

- 加载状态增加 renderer、downloading、parsing 阶段和可计算的资源下载比例；页面文案改为当前模型语义。
- 状态栏显示初始化中、WebGPU 已就绪、不可用和设备丢失，不再在错误状态固定显示 WebGPU。
- 增加重试 token 和“重试当前模型”入口；覆盖 WebGPU 初始化失败、模型加载失败和设备丢失后的重新初始化。

## T4 操作语义与无障碍

**状态：已完成。**

### 方案

1. 在信息架构上明确分为“观察相机”“飞机姿态”“工作室渲染”三类。相机快捷视图和姿态预设不混用同一重置逻辑。
2. 将当前同时控制俯仰和偏航的 `role="slider"` 拆为语义独立的原生 range 或等价可访问控件；滚转同样使用单一可读数值。直接拖拽 gizmo 保留为增强交互，而不是唯一操作入口。
3. 为相机提供键盘可达的快捷视角、适配视图和恢复视图。canvas 保持装饰性时，外围 HTML 必须完整表达当前模型名、相机视图、姿态和渲染状态。
4. 所有面板使用稳定且实例安全的 `useId` 关联 `aria-controls`，并保证每一个按钮、range、switch 和错误重试都有可读名称与焦点状态。
5. 移动端工具按钮与可直接点击的控制目标不小于 44px；状态变化不只依赖颜色，当前面板、选中预设和异常状态同时使用文本或结构提示。
6. `prefers-reduced-motion` 下停止 loading 旋转和所有非必要的持续运动；直接操控仍应即时跟随手势。

### 影响文件

- `src/pages/planeRender/AircraftModelViewport.tsx`
- `src/pages/planeRender/components/RenderControls.tsx`
- `src/pages/planeRender/index.css`

### 验收条件

- 不使用鼠标时，可完成模型选择、相机恢复、相机快捷视图、姿态调整、渲染设置、面板关闭和全屏进出。
- 读屏软件可读取当前模型、三个姿态角、活动面板、加载阶段、错误原因和重试动作。
- 减少动态偏好下不出现旋转加载器或持续自动渲染动画。

### 本次实现

- 姿态 gizmo 改为语义 group，增加独立的俯仰、滚转、偏航原生 range；保留指针拖拽作为直接操控增强。
- 渲染控制面板及其控件使用 `useId` 生成实例安全的 DOM id；姿态读数提供 polite live 更新。
- 移动端工具按钮、预设、select、重置和 range 扩大到 44px 触控高度，标准相机视角菜单可直接键盘操作。

## T5 性能与画质预算

**状态：已完成。**

### 方案

1. 将持续 `setAnimationLoop` 改为按需绘制：模型加载完成、ResizeObserver 回调、设置变化、控制器阻尼未停止和手势进行中才请求下一帧。
2. 页面不可见或视窗不在屏幕内时暂停绘制；重新可见时重新 fit 或至少补一帧，保证画面不会陈旧。
3. 默认像素倍率采用设备像素比与安全上限的较小值，而不是所有设备固定 `2x`；控制面板改为“性能、均衡、高质量”预设，并保留高级自定义倍率。
4. 滑动姿态和高成本渲染设置时，通过 `requestAnimationFrame` 合并视觉更新，避免每个 pointer event 同时触发 React 更新、Three.js 更新和缓冲区重建。
5. 在性能面板展示简单、可信的状态，例如当前质量预设和降低画质的原因。不要把内部帧率、GPU 型号等不稳定实现细节作为常规用户界面。
6. 后续模型缓存采用有上限的 LRU，缓存键使用最终资源 URL；淘汰和页面卸载必须沿用同一资源释放路径。该项在用户频繁切换大模型时再进入实施。

### 影响文件

- `src/pages/planeRender/AircraftModelViewport.tsx`
- `src/pages/planeRender/components/RenderControls.tsx`
- `src/pages/planeRender/index.css`

### 验收条件

- 静止、隐藏和错误场景不会保留无意义的高频渲染循环。
- 在高 DPI 桌面和移动设备上切换质量预设时，画布尺寸、清晰度和交互均稳定。
- 连续拖动姿态控件时，模型和 gizmo 同步，无明显拖尾或输入延迟。

### 本次实现

- 使用按需 `requestAnimationFrame` 驱动渲染，OrbitControls 阻尼结束后停止排帧；尺寸、设置、模型和控制器变化会主动请求绘制。
- 页面隐藏或 viewport 离开可视区域时取消绘制，恢复可见时补帧；清理时同步断开观察器和待执行帧。
- 默认像素倍率按设备 `devicePixelRatio` 限制在 2x 以内，手动范围收窄为 `0.5..3`，并增加性能优先、均衡、质量优先和自定义预设。

## T6 画面质感与照明预设

**状态：已完成。**

### 方案

1. 建立可复用的工作室照明预设，例如“中性检查”“轮廓检查”“性能检查”，替代要求用户直接理解三轴灯光坐标。
2. 为 PBR 材质提供可控环境反射来源。优先使用本地、可打包的低成本环境方案或程序化工作室环境，禁止加载无来源的外部 HDR 资源。
3. 让场景背景、地面和照明颜色响应项目深浅主题；保留足够对比，避免深色机身在深色背景中丢失轮廓。
4. 处理阴影与展示平面的依赖：展示平面隐藏时，不应让“实时阴影已开启”看起来毫无效果。可采用默认展示接触基准、说明依赖关系或将其收敛为单个工作室预设。
5. 维持当前克制的档案型视觉，不增加无关自动旋转、重度模糊或装饰性特效。

### 验收条件

- 白色、深色、金属和玻璃材质均能区分基本形状与关键细节。
- 深浅主题切换后，模型边缘、状态文字和工具层保持足够对比。
- 预设切换不重新加载 GLB，也不会损坏用户已经选择的相机和姿态。

### 本次实现

- 使用本地 `RoomEnvironment` 和 WebGPU `PMREMGenerator` 生成环境反射，设置场景 `environmentIntensity = 0.45`，不引入外部 HDR 资源。
- 增加中性检查、轮廓检查和顶部检查照明预设；手动移动主光源后自动标记为自定义。
- 在 `App.css` 增加深浅主题下的模型地面、主光和补光 token，并通过 `MutationObserver` 实时同步到 Three.js 场景。
- 阴影开关在展示平面关闭时显示依赖状态，避免用户误以为阴影没有生效。

## T7 渲染后产出能力

**状态：已完成。**

### 方案

1. 提供导出当前视图截图的命令，文件名包含模型标识和时间；导出前确认 WebGPU canvas 的实际截图支持与透明背景策略。
2. 提供“复制检查设置”或下载 JSON：包含模型 ID、相机视图、姿态、工作室预设和高级渲染参数。只保存可复现的用户设置，不保存浏览器或 GPU 私有信息。
3. 若加载的 GLB 存在 `animations`，显示播放、暂停和时间位置控制；没有动画时不展示空控件。
4. 多机型对比、标注和分享链接属于后续需求，须先定义数据量、URL 长度、隐私边界和移动端交互，不在截图导出任务中顺带实现。

### 验收条件

- 导出的截图与当前可视画面一致，不包含无关页面内容。
- 导入或复用设置后，可恢复同一模型、姿态和渲染预设。
- 无动画模型不出现不可用的播放控件。

### 本次实现

- 工具条增加 PNG 导出和设置 JSON 导出；设置包含模型 ID、相机位置/目标、相机视角、姿态和渲染参数。
- 加载带动画的 GLB 时创建 `AnimationMixer`，显示单段动画的播放/暂停、时间轴和当前时间；无动画模型不展示控制器。
- 截图和设置下载均通过浏览器 Blob URL，下载完成后释放临时 URL；失败时在视窗内显示可读错误。

## 文件变更边界

| 文件 | 预期职责变化 |
| --- | --- |
| `src/pages/planeRender/AircraftModelViewport.tsx` | 场景层级、相机 fit、姿态四元数、全屏目标协作、渲染调度、加载和失败状态 |
| `src/pages/planeRender/index.tsx` | 页面级 fullscreen target、状态展示、模型切换策略和重试入口 |
| `src/pages/planeRender/components/RenderControls.tsx` | 质量和照明预设、高级设置、控件语义 |
| `src/pages/planeRender/index.css` | 工具层布局、全屏覆盖、移动端底部面板、触摸区域和减少动态样式 |
| `docs/aircraft-model-viewport.md` | 实施后同步真实架构、状态契约和验收路径 |
| `taskRecord.md` | 每个实施任务追加实际修改和验证结果 |

## 验证矩阵

实施每个任务后先执行：

```bash
pnpm run type-check
pnpm run build
git diff --check
```

浏览器验收覆盖以下场景：

| 场景 | 必查行为 |
| --- | --- |
| 桌面 WebGPU | 默认加载、相机六视图、姿态五个固定姿态、照明预设、质量切换 |
| 窄屏 320px | 面板不裁切、单面板互斥、目录滚动、触摸旋转与页面滚动共存 |
| 全屏 | 实际全视口覆盖、元信息可见、退出按钮、Esc、焦点返回、窗口尺寸变化 |
| 错误路径 | 无 WebGPU、初始化失败、资源 URL 失败、损坏 GLB、重试成功或失败 |
| 无鼠标 | 模型选择、相机恢复、姿态、渲染设置、面板关闭、全屏进出 |
| 减少动态 | 无 loading 旋转和无意义持续动画，直接操作仍即时反馈 |
| 性能回归 | 静止/不可见时暂停渲染，高质量预设不导致画布尺寸错误或明显输入阻塞 |

## 不在本轮内的事项

- WebGL 自动降级。
- 修改或重建 GLB 模型、纹理和 UV。
- 多模型同时渲染、模型搜索或目录数据管理。
- 云端保存、协作标注和服务端分享。
